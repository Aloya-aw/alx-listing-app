// constants/index.ts

// Base API URL
export const API_BASE_URL = 'https://api.example.com'

// UI Text
export const UI_TEXT = {
  bookNow: 'Book Now',
  details: 'View Details',
  noListings: 'No listings available at the moment.',
}

// App configuration
export const APP_CONFIG = {
  defaultCurrency: 'USD',
  maxListingPerPage: 10,
}
